{
  "name": "dotagents",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "bunx lefthook install",
    "format": "bunx prettier --write .",
    "format:check": "bunx prettier --check .",
    "lint": "bunx eslint .",
    "bash:format": "find scripts skills -type f -name '*.sh' ! -path 'skills/.system/*' ! -path 'skills/projects/*' -exec shfmt -w {} +",
    "bash:format:check": "find scripts skills -type f -name '*.sh' ! -path 'skills/.system/*' ! -path 'skills/projects/*' -exec shfmt -d {} +",
    "bash:lint": "find scripts skills -type f -name '*.sh' ! -path 'skills/.system/*' ! -path 'skills/projects/*' -exec shellcheck {} +",
    "check": "bun run lint && bun run format:check && bun run bash:lint && bun run bash:format:check"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "eslint": "^9.34.0",
    "eslint-config-prettier": "^10.1.0",
    "jiti": "^2.5.0",
    "lefthook": "^1.12.7",
    "prettier": "^3.6.2"
  },
  "prettier": {
    "semi": false,
    "singleQuote": false,
    "useTabs": false,
    "trailingComma": "all",
    "arrowParens": "avoid",
    "printWidth": 120,
    "endOfLine": "lf"
  }
}
